# Development Environment Variables

# SpacetimeDB
SPACETIME_MODULE_NAME=kanban-plus
SPACETIME_PORT=3000
SPACETIME_ADMIN_PORT=3001

# Client
CLIENT_PORT=5173
NODE_ENV=development
VITE_SPACETIME_URI=ws://spacetimedb:3000/kanban-plus

# Puppeteer
PUPPETEER_PORT=9222
SCREENSHOT_DIR=/screenshots
ERROR_LOG_DIR=/errors
CLIENT_URL=http://client:5173

# Docker Project Name (for isolated environments)
# Override with: docker-compose -p <name> up
COMPOSE_PROJECT_NAME=spacetimedb-kanban